'use strict';

/**
 * @class
 * @name Server
 * @description Initialize a new server
 * @example
 * const gIPC = require("generic-ipc");
 * let instance = new gIPC(3000, {authorization: true})
 */
class Server{
    constructor(){
        
    }
}